export enum Resource {
  WORKSPACE = "workspace",
  BOARD = "board",
  TASK = "task",
}

export enum Action {
  CREATE = "create",
  VIEW = "view",
  UPDATE = "update",
  DELETE = "delete",
}

export enum Role {
  OWNER = "OWNER",
  ADMIN = "ADMIN",
  MEMBER = "MEMBER",
  VIEWER = "VIEWER",
}
