export enum Resource {
  WORKSPACE = "workspace",
  BOARD = "board",
  TASK = "task",
}

export enum Action {
  CREATE = "create",
  VIEW = "view",
  UPDATE = "update",
  DELETE = "delete",

  INVITE = "invite",
  REMOVE = "remove",
  ARCHIVE = "archive",
  ASSIGN = "assign",
}
